// MyProgressBar.tsx
import { extendVariants, Progress } from "@heroui/react";

export const MyProgress = extendVariants(Progress, {
  variants: {
    color: {
    },
  },
  defaultVariants: {
  },
});
