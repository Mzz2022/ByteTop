// MyDropdown.tsx
import { Dropdown, extendVariants } from "@heroui/react";

export const MyDropdown = extendVariants(Dropdown, {
  variants: {
    variant: {
    },
  },
  defaultVariants: {
  },
});
